org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

version = 12.3.0
maven_group = net.ludocrypt
archives_base_name = limlib

# soft-dependencies, to make sure the library doesn't break from using certain performance and shader mods
